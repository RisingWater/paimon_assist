# 环境配置（含密钥，不要提交）
.env

# Python 虚拟环境
venv/

# SenseVoiceSmall 模型（大文件，首次运行自动下载）
models/iic/

# VITS 模型（大文件）
models/paimon.pth
models/G_1434000.pth

# 声纹数据库（首次运行自动生成）
models/voiceprints.db

# TTS 缓存（自动生成，避免重复合成）
models/tts_cache/

# 录音文件
*.wav

# Python
__pycache__/
*.pyc
.python-version
